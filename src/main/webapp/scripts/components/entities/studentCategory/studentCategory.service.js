'use strict';

angular.module('try1App')
    .factory('StudentCategory', function ($resource, DateUtils) {
        return $resource('api/studentCategorys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
